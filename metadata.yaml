name: cinder-pure
summary: Pure Storage FlashArray integration for OpenStack Block Storage
maintainers:
 - Simon Dodsley <simon@purestorage.com>
description: |
 Cinder is the block storage service for the Openstack project.
 .
 This charm provides an Pure Storage Flasharray storage backend for Cinder
tags:
  - openstack
  - storage
subordinate: true
provides:
  storage-backend:
    interface: cinder-backend
    scope: container
requires:
  juju-info:
    interface: juju-info
    scope: container
